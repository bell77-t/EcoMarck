import { request, notify } from './app.js';
document.addEventListener('DOMContentLoaded',()=>{const form=document.querySelector('[data-auth-form]');if(!form)return;form.addEventListener('submit',async(e)=>{e.preventDefault();const button=form.querySelector('button[type=submit]');button.disabled=true;try{const data=Object.fromEntries(new FormData(form));if(form.dataset.authForm==='register'){await request('/register',{method:'POST',body:JSON.stringify(data)});notify('Cuenta creada. Ahora inicia sesión.');location.href='login.html';}else{const out=await request('/login',{method:'POST',body:JSON.stringify(data)});localStorage.setItem('ecoToken',out.token);localStorage.setItem('ecoUid',out.uid);location.href='catalog.html';}}catch(error){notify(error.message,'danger');}finally{button.disabled=false;}});});
