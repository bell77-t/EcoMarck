import { request, notify, escapeHtml, placeholder } from './app.js';
document.addEventListener('DOMContentLoaded',async()=>{if(!localStorage.getItem('ecoToken'))return location.href='login.html';const form=document.querySelector('#profileForm');try{const p=await request('/profile');form.nombre.value=p.nombre;form.foto.value=p.foto||'';document.querySelector('#email').textContent=p.correo;document.querySelector('#avatar').src=p.foto||placeholder;}catch(e){notify(e.message,'danger');}form.foto.addEventListener('input',()=>document.querySelector('#avatar').src=form.foto.value||placeholder);form.addEventListener('submit',async(e)=>{e.preventDefault();try{const out=await request('/profile',{method:'PUT',body:JSON.stringify(Object.fromEntries(new FormData(form)))});document.querySelector('#avatar').src=out.foto||placeholder;notify('Perfil actualizado.');}catch(error){notify(error.message,'danger');}});});
