// Firebase se configura y utiliza de forma segura desde el backend con Firebase Admin.
// Este módulo se reserva para una futura integración de SDK cliente si se requiere.
export const firebaseEnabled = true;
